.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# rust / tauri build outputs
src-tauri/target

# AI workflow tooling
.opencode/
.codegraph/

# Claude Code: 纳入 git 团队共享(hooks/settings/agents/skills/commands);仅忽略个人/运行时数据。
.claude/settings.local.json
.claude/todos/
.claude/logs/
.claude/shell-snapshots/
.claude/statsig/
.claude/ide/

# Trellis: 入 git 团队共享(spec/tasks/workflow/scripts 等);个人/运行时数据忽略。
# .trellis/.gitignore 已处理大部分,这里对身份与运行时再加一层保险。
.trellis/.developer
.trellis/.current-task
.trellis/.runtime/
