/** 模板变量占位符(与后端 llm.rs 的 TPL_* 常量一致)。 */
export const TPL_DATE = '{{date}}';
export const TPL_INPUT = '{{input}}';
export const TPL_CONV = '{{conversations}}';
export const TPL_DATE_RANGE = '{{date_range}}';
export const TPL_DAY_SUMMARIES = '{{day_summaries}}';

export const DEFAULT_PROMPT_TEMPLATE = `你是日报整理助手。请综合下面的「今日工作要点」与「今日 AI 工具对话记录」，整理成如下格式的日报：

{{date}}日报：

## 工作事项
1、...
2、...

## 遇到问题
...

总结
...

要求：

「工作事项」用「数字、」编号（如 1、2、），提炼成清晰条目，并且每一组需要换行

「遇到问题」如实总结阻碍，没有就写「无」

「总结」用一两句话概括今日工作的核心思路或价值，专业简练

若「对话记录」为空，仅依据「工作要点」生成；两者都有时以对话记录为主、要点为补充

严格输出：只输出上述日报格式的内容，开头不添加任何问候语、确认语、解释语（如"好的""已为您整理"等），结尾不添加任何总结或询问语，直接生成日报正文。

今日工作要点：
{{input}}

今日 AI 工具对话记录（参考素材，可能为空）：
{{conversations}}`;

/** 周报 map（每日摘要）默认提示词（与 Rust `WEEKLY_MAP_PROMPT` 兜底常量文本一致）。 */
export const DEFAULT_WEEKLY_MAP_TEMPLATE = `你是工作摘要助手。请把下面「某一天」的 AI 工具对话记录提炼成一份简洁的当日工作摘要，供后续汇总周报使用。

日期：{{date}}

当日 AI 工具对话记录：
{{conversations}}

要求：
- 用条目列出当天实际做了哪些事（实现/修复/调研/重构等），合并琐碎细节，突出进展与成果；
- 简要标注遇到的关键问题或阻塞（没有则不写）；
- 控制在 150 字以内，不要复述对话原文，不要寒暄或解释，只输出摘要本身。`;

/** 周报 reduce（整周汇总）默认提示词（与 Rust `WEEKLY_REDUCE_PROMPT` 兜底常量文本一致）。 */
export const DEFAULT_WEEKLY_REDUCE_TEMPLATE = `你是周报整理助手。请把下面本周各天的「当日工作摘要」跨天归纳成一份整周周报。

本周区间：{{date_range}}

本周补充要点（用户手写，可能为空）：
{{input}}

本周各日工作摘要：
{{day_summaries}}

要求格式：

{{date_range}}周报：

## 本周工作事项
1、...
2、...

## 遇到问题
...

## 总结
...

要求：
- 「工作事项」跨天合并同类项，按主题/项目组织，用「数字、」编号，每组换行；提炼成果与进展，不要堆砌流水账；
- 「遇到问题」如实归纳本周的主要阻碍，没有就写「无」；
- 「总结」用一两句话概括本周工作的核心思路或价值，专业简练；
- 若「补充要点」非空，将其纳入对应主题；
- 严格只输出周报正文，开头不加问候/确认/解释，结尾不加总结性询问。`;
